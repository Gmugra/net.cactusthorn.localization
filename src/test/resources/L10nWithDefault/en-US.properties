#test file 
#warning: UTF-8 encoded  

# system properties section
_system.id = test-app
_system.languageTag = en-US
_system.escapeHtml = false

# formats
_format.dt3.type = datetime
_format.dt3.pattern = yyyy-MM-dd'T'HH

# translations

# simple parameters
test.param.first = FIRST: {{first}}, second:{{second}}<br/>
test.param.second$html = {{second}}, {{second}}, {{second}}; <b>{{first}}, {{first}}, {{first}};</b>

x.y.z.apple = apples by default
x.y.z.apple.0 = no any apples
x.y.z.apple.1 = one apple
x.y.z.apple.22$html = special case:<br/> 22 apples
x.y.z.apple.$1$html = {{count}}<br/> apples

datetime_text_one = datetime={{dt,dt1}}
datetime_text_two = datetime={{dt,dt3}}